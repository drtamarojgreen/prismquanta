# --- Configuration ---
# All paths are relative to the project root directory where the executable is run.

LOG_FILE = logs/interface.log
PROMPT_FILE = prompts/input_prompt.txt
TIMEOUT_MARKER = logs/.timeout
RULES_FILE = rules/rules.xml
PRIORITY_FILE = priorities.txt
POLL_INTERVAL_SEC = 60
TIMEOUT_DURATION_SEC = 7200
